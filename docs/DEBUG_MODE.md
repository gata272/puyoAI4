# デバッグモード

通常プレイと研究用機能を分離するため、設定画面からデバッグモードを切り替えられます。

- OFF: 通常プレイUIのみ。ベンチマークUIを隠す。
- ON: 最大連鎖ベンチマークUIを表示。

設定はブラウザの `localStorage` に保存され、再読み込み後も維持されます。

ベンチマークは `benchmark-worker.js` という別Workerで実行され、通常のAI自動操作Workerとは分離されています。
